# ═══════════════════════════════════════════════════════════════════════════
#  ACM — Docker Compose
#  Autonomous Constellation Manager | National Space Hackathon 2026
# ═══════════════════════════════════════════════════════════════════════════

version: '3.8'

services:
  # ── FastAPI Backend & Dashboard (Python) ─────────────────────────────
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      # NOTE: engine/ is intentionally NOT mounted—host files would shadow the
      # compiled autocm_engine.so baked into the image during the build stage.
      - ./data:/app/data
      - ./frontend:/app/frontend
    environment:
      - ENV=development
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
